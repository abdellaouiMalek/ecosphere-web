{% extends 'base.html.twig' %}

{% block title %}Edit Carpooling{% endblock %}

{% block body %}
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">{{ block('title') }}</div>

                <div class="card-body">
                    <h1>Edit Carpooling</h1>

                    {{ include('carpooling/_form.html.twig', {'button_label': 'Update'}) }}
                    
                    <a href="{{ path('app_home') }}" class="btn btn-secondary mt-3">Back to List</a>
                </div>
            </div>

            <div class="card mt-3">
                <div class="card-header">Delete Carpooling</div>

                <div class="card-body">
                    {{ include('carpooling/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
