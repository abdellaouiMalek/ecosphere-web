{% extends 'base.html.twig' %}

{% block title %}Historique{% endblock %}

{% block body %}
    <h1>Historique</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ historique.id }}</td>
            </tr>
           <tr>
                <th>Nom</th>
                <td>{{ historique.nom_o}}</td>
            </tr>
            <tr>
                <th>InitialCondition</th>
                <td>{{ historique.initialCondition }}</td>
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('app_historique_index') }}">back</a>

    <a href="{{ path('app_historique_edit', {'id': historique.id}) }}">edit</a>

    {{ include('historique/_delete_form.html.twig') }}
{% endblock %}
